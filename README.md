## NMEA GPS Parser

### Usage

Configure the input file by replacing ```FILE_PATH``` with your raw input file.

Configure the output file name by replacing  ```OUTPUT_FILE ``` with your prefered output file name

To run the parser ``` python nmea_parser.py ```